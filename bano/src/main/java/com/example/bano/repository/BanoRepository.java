package com.example.bano.repository;

import com.example.bano.entity.Bano;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BanoRepository extends JpaRepository<Bano, Integer> {
}
