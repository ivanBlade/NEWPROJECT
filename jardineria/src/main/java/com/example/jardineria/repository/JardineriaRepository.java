package com.example.jardineria.repository;

import com.example.jardineria.entity.Jardineria;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JardineriaRepository extends JpaRepository<Jardineria, Integer>  {
}
